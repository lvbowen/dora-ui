---
name: Spin 加载中
route: /Spin
menu: 组件
---

import CodeBox from '../../utils/CodeBox';

import BasicDemo from './demo/1-demo-basic';
import BasicDemoCode from '!raw-loader!./demo/1-demo-basic';

import FullDemo from './demo/2-demo-full';
import FullDemoCode from '!raw-loader!./demo/2-demo-full';

import CustomizeDemo from './demo/3-demo-customize';
import CustomizeDemoCode from '!raw-loader!./demo/3-demo-customize';

# Spin 加载中

用于页面和区块的加载中状态。

## 代码演示

### 基本用法

<CodeBox code={BasicDemoCode}>
  <BasicDemo></BasicDemo>
</CodeBox>

### 全屏加载

<CodeBox code={FullDemoCode} title="全屏加载" desc="全局loading时使用">
  <FullDemo></FullDemo>
</CodeBox>

### 自定义 Spinner

默认 Spinner 为 `<Spinner size="md" type="wave">`，可以根据业务需求编写自己的 Spinner。

<CodeBox code={CustomizeDemoCode} title="更换Spinner">
  <CustomizeDemo></CustomizeDemo>
</CodeBox>

### delay 延迟

延迟显示 loading 效果。当 spinning 状态在 delay 时间内结束，则不显示 loading 状态。

常用于解决异步请求时间过短或较短时间内多个异步请求依次发起结束造成的闪烁。

如设置为 1000ms，`spinning`属性若在 1000ms 内由`true`变为`false`，不会展示 Spinner。

## API

| 属性             | 说明                                               | 类型            | 默认值                            |
| ---------------- | -------------------------------------------------- | --------------- | --------------------------------- |
| spinning         | 是否展示 Spinner                                   | boolean         | -                                 |
| fullScreen       | 是否一屏（如果加载的内容高于一屏，建议设置此选项） | boolean?        | true                              |
| children         | 被包裹的内容                                       | ReactNode       | -                                 |
| tip              | 描述文案                                           | string?         | ''                                |
| spinner          | 自定义 Spinner                                     | ReactElement?   | `<Spinner size="md" type="wave">` |
| size             | Spinner 大小，仅对默认 Spinner 有效                | 'sm'/'md'/'lg'? | 'md'                              |
| wrapperClassName | 包装器的类属性                                     | string?         | ''                                |
| transition       | 是否展示过渡动画                                   | boolean?        | true                              |
| delay            | 延迟显示加载效果的时间，毫秒                       | number?         | 0                                 |
